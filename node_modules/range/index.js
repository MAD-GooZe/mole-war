module.exports = function(start, stop) {
    //TODO: write a better range function
    if (typeof(stop) == "undefined") {
        stop = start;
        start = 0;
    }
    var result = [];
    for (var i = start; i < stop; i++)
        result.push(i);
    return result;
};